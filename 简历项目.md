# LiteAI — SRE 故障处置 Agent

## 项目简介

面向一线 SRE / 值班的事故诊断 Agent，提供「告警响应 → 多源并行取证 → 假设-验证 → 检查点交接 → 复盘归档 → 事故知识库检索」的功能闭环，解决多源观测数据下根因定位慢、跨班交接无据可循的问题；基于标准 ReAct 循环（ModelAdapter.next() + ToolRegistry.execute() 两个极小接口）构建 agent 内核，以工具注册表、并行子 agent 编排与多级上下文压缩支撑长会话、低 token 开销地定位根因。

## 技术栈

TypeScript、Node.js、ReAct / Re-Prompt 引导、多 agent 编排（spawn_agent / wait_agent）、ModelAdapter.next() 抽象层（OpenAI / Anthropic 适配）、Prometheus / ES / Kubernetes / 只读 SQL toolset、sqlite-vec + 本地 embedding 语义检索、zod、tsc + eslint + 单测/tool-concurrency 集成测试

## 项目产出

- **多源并行子 agent 编排**：提供 spawn_agent / wait_agent 原语，命中 ≥2 个独立数据源 / 服务时自动并发只读子 agent 各自归口取证再合并，并对子 agent 做工具白名单约束。
- **上下文压缩策略**：snip / microcompact / collapse / auto-compact 多级压缩，microcompact 按 isReadOnly 自动折叠旧只读工具结果为占位符（无需维护前缀名单），工具输出 clamp 到 8k 并摘要化列表，控制长会话体积、延长单次故障诊断容量。
- **本地语义检索与知识库**：用 sqlite-vec + 本地 embedding（@huggingface/transformers）实现相似历史事故检索（search_incident_kb），复盘可引用历史案例，形成可沉淀的领域 RAG。
- **会话管理**：按项目（cwd）聚类持久化会话，采用 append-only JSONL 落盘（事件只增写盘 + UUID 幂等去重 + parentUuid 链保留时间线），重复保存/崩溃恢复不产生重复事件、可直接 tail / grep 审计；支持 resume / fork / rename / 过期清理，告警自动诊断产物直接落为可 resume 的会话，值班员可随时接管继续排查。