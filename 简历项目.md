# LiteAI — SRE 故障处置 Agent

## 项目简介

面向一线 SRE / 值班的事故诊断 Agent，提供「告警响应 → 多源并行取证 → 假设-验证 → 检查点交接 → 复盘归档 → 事故知识库检索」的功能闭环，解决多源观测数据下根因定位慢、跨班交接无据可循的问题；基于标准 ReAct 循环（ModelAdapter.next() + ToolRegistry.execute() 两个极小接口）构建 agent 内核，以工具注册表、并行子 agent 编排与多级上下文压缩支撑长会话、低 token 开销地定位根因。

## 技术栈

TypeScript、Node.js、ReAct / Re-Prompt 引导、多 agent 编排（spawn_agent / wait_agent）、ModelAdapter.next() 抽象层（OpenAI / Anthropic 适配）、Prometheus / ES / Kubernetes / 只读 SQL toolset、sqlite-vec + 本地 embedding 语义检索、zod、tsc + eslint + 单测/tool-concurrency 集成测试

## 项目产出

### 1. 多源并行子 agent 编排

- **S 情境**：一次事故常跨多套观测系统（Prometheus / ES / K8s），串行逐个查询会明显拉低定位速度
- **T 任务**：让 agent 在多源场景下并行取证，同时守住只读边界
- **A 行动**：实现 spawn_agent / wait_agent 原语，命中多个独立数据源时按系统提示并行只读子 agent 各自归口取证再合并；子 agent 由系统提示限定只读、不能改文件 / 建子 agent，并受 run_command 只读强制约束
- **R 结果**：多源取证并行执行，避免串行拖慢根因定位；定位耗时相对串行 `{{实测并行加速比}}X`（待实测）

### 2. 上下文压缩策略

- **S 情境**：长会话中历史消息与工具结果不断累积，容易占满上下文窗口
- **T 任务**：在受限上下文内支撑 agent 连续多步取证、低 token 开销
- **A 行动**：分层落地 snip / microcompact / collapse / auto-compact 多级压缩；microcompact 按 isReadOnly 自动折叠旧只读工具结果为占位符（无需维护前缀名单）；工具输出 clamp 到 8k 并对列表摘要化
- **R 结果**：有效控制长会话体积，延长单次故障诊断容量；压缩后 token 节省 `{{实测节省}}%`（待实测）

### 3. 本地语义检索与知识库

- **S 情境**：复盘时难引用历史事故，同类故障的处置经验难沉淀复用
- **T 任务**：让相似历史事故可被自然语言检索
- **A 行动**：用 sqlite-vec + 本地 embedding（@huggingface/transformers）实现 search_incident_kb 语义检索
- **R 结果**：诊断前可召回相似历史事故，形成可沉淀的领域 RAG

### 4. 会话管理

- **S 情境**：值班排查过程易丢、跨班交接无据可循、全程不可审计
- **T 任务**：让排查过程可恢复、可交接、可审计
- **A 行动**：按项目（cwd）聚类持久化，采用 append-only JSONL 落盘（事件只增写盘 + UUID 幂等去重 + parentUuid 链保留时间线）；支持 resume / fork / rename / 过期清理；告警自动诊断产物直接落为可 resume 会话
- **R 结果**：重复保存 / 崩溃恢复不产生重复事件，可直接 tail / grep 审计，值班员可随时接管继续排查

### 5. 假设-验证与交接机制

- **S 情境**：根因定位过程不透明化为「值班脑中的临时记忆」，复盘与交接都缺依据
- **T 任务**：把排查过程结构化为可交接、可复盘的对象
- **A 行动**：hypothesis_tracker 用 pending / investigating / confirmed / refuted / inconclusive 维护结构化假设链并为每条结论附着可追溯证据；incident_checkpoint 一键生成跨班交接简报（现象 / 时间线 / 已排除 / 待验证 / 关键命令）
- **R 结果**：跨班交接有据可循，复盘可溯源到证据与命令

## 我的角色

（可选：在此补充你在该项目中的负责范围 / 团队规模 / 或整体影响的一句话，例如「独立完成核心内核与诊断链路设计」。）

---

## 核心产出（极简版 · 简历用）

- **多源并行取证**：spawn_agent / wait_agent 编排并行只读子 agent 归口取证，避免多源串行拖慢根因定位
- **长会话能力**：snip / microcompact / collapse / auto-compact 多级上下文压缩（只读结果自动折叠、输出 clamp 8k）+ append-only JSONL 会话（resume / fork / 审计）
- **领域知识库**：sqlite-vec + 本地 embedding 语义检索历史事故（search_incident_kb）
- **假设-验证与交接**：hypothesis_tracker 维护结构化假设链并附着证据，incident_checkpoint 一键生成跨班交接简报