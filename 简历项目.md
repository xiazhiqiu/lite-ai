# LiteAI —— 面向 SRE 的事故诊断 Agent

> 简历项目 · 负责身份：核心架构 & agent 内核 / 事故诊断链路设计 / 多源并行编排 / 上下文工程
> 项目地址/形态：开源项目 · TypeScript / Node.js · Terminal-first 单机部署

---

## 一句话说明

一款面向一线 SRE / 值班的事故诊断 Agent，用标准 ReAct 循环打通**告警响应 → 多源并行取证 → 假设-验证 → 检查点交接 → 复盘归档 → 事故知识库检索**的功能闭环，解决多源观测数据下「根因定位慢、跨班交接无据可循」两个生产痛点。

---

## 我负责的核心工作

### 1. agent 循环内核（模型适配 + 工具注册表两个极小接口）

- 围绕 `ModelAdapter.next()` 与 `ToolRegistry.execute()` 两个极小接口实现单轮多步工具调用，代理层抽象兼容 OpenAI / Anthropic，并支持 `mock` 离线模式与 thinking 回传，便于离线评测与快速迭代。
- 超大工具结果自动落盘，在上下文里替换为短预览与文件路径，保证 agent 在受限上下文内可持续多步取证。

### 2. 多源并行子 agent 编排（spawn_agent / wait_agent）

- 命中 ≥2 个独立数据源 / 服务时自动并发只读子 agent 各自归口取证再合并，避免串行拉低定位速度。
- 对子 agent 做工具白名单约束，控制其在只读诊断边界内工作。

### 3. 多级上下文压缩（snip / microcompact / collapse / auto-compact）

- `microcompact` 按 `isReadOnly` 自动折叠旧只读工具结果为占位符（无需维护前缀名单）。
- 工具输出 clamp 到 8k 并摘要化列表，多级配合控制长会话体积，延长单次故障诊断容量。

### 4. 只读安全边界

- 数据源工具统一 `isReadOnly`；`run_command` 仅放行 SRE 只读诊断命令，kubectl / docker / curl 按子命令白名单拦截写操作；通用文件工具不暴露给 agent，防止越权读取敏感数据。

### 5. 本地语义检索与知识库 RAG

- 用 sqlite-vec + 本地 embedding（@huggingface/transformers）实现相似历史事故检索 `search_incident_kb`，复盘可引用历史案例，沉淀领域 RAG。

### 6. 事故诊断链路（业务闭环）

- 告警侧：独立的 webhook 常驻进程接收 Alertmanager 等事件源，去重 → 硬截断 → 串行队列自动诊断 → 存会话 → 回推通知。
- 排查工具集：`hypothesis_tracker`（假设-验证链）、`incident_checkpoint`（跨班交接简报）、`generate_postmortem`（复盘归档）、`tail_logs`（流式日志）。

### 7. 会话管理

- 按项目（cwd）聚类持久化会话（jsonl），支持 resume / fork / rename / 按项目聚类 / 过期清理，运行状态全程可审计、可恢复。
- 告警自动诊断产物直接落为可 resume 的会话，值班员可随时接管继续排查，跨班交接有据可循。

---

## 技术栈

| 维度 | 选型 |
|---|---|
| 语言 / 运行时 | TypeScript, Node.js |
| agent 框架自研 | ReAct / Re-Prompt、多 agent 编排（spawn_agent / wait_agent）、ModelAdapter.next() 抽象（OpenAI / Anthropic / mock） |
| 数据源 toolset | Prometheus / Elasticsearch / Kubernetes / 只读 SQL / Tempo / Loki |
| 检索 / RAG | sqlite-vec + 本地 embedding（@huggingface/transformers） |
| 校验 / 质量 | zod、tsc、eslint、单测 & tool-concurrency 集成测试 |

---

## 可量化的成果 / 验证

- 接入 RE2-SS 故障集与 ITBench（真实 K8s + otel-demo + 故障注入）端到端评测，可验证多源并行相对串行方案对根因定位效率的提升。
- 通过多级上下文压缩，将长会话体积控制在低 token 开销，延长单次故障诊断容量。
- 全程只读安全边界 + 全量运行状态可审计 / 可 resume / fork，可直接用于生产值班机。

---

## 我的工程收获 / 亮点

- 以「两个极小接口」为内核做顶层抽象，换来可替换的模型层、可插拔的数据源、可约束的并行编排——验证了符合 REPL 型 agent 的高内聚低耦合设计。
- 把「长会话上下文紧张」这一通用 agent 难题，落地为一套可审计、可恢复、不依赖前缀名单的压缩体系。
- 面向真实故障处置的业务闭环设计（从告警到复盘），而非只做 demo 型 tool-call。