# LiteAI — SRE 故障处置 Agent（简洁版）

面向一线 SRE 的事故诊断 Agent，打通「告警响应 → 多源并行取证 → 假设-验证 → 检查点交接 → 复盘归档 → 事故知识库检索」闭环，基于标准 ReAct 循环（ModelAdapter.next() / ToolRegistry.execute() 两个极小接口）构建内核，解决多源数据下根因定位慢、跨班交接无据可循的问题。

**技术栈**：TypeScript / Node.js · ReAct + 多 agent 编排 · OpenAI / Anthropic 适配 · Prometheus / ES / K8s / 只读 SQL toolset · sqlite-vec 本地 embedding RAG

**重点产出**：

- **agent 内核与多源并行编排**：两个极小接口实现单轮多步工具调用；spawn/wait_agent 在命中 ≥2 个数据源时自动并发只读子 agent 归口取证再合并并做白名单约束；接入 RE2-SS 故障集端到端评测验证并行定位效率。
- **长会话能力**：snip / microcompact / collapse / auto-compact 多级上下文压缩，按 isReadOnly 折叠只读工具结果、工具输出 clamp 8k，控制长会话体积；会话持久化支持 resume / fork / rename，告警诊断会话可随时接管，全程可审计。
- **领域知识库 RAG**：sqlite-vec + 本地 embedding 实现历史事故语义检索，复盘引用历史案例。